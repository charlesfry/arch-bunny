#!/bin/bash
set -euo pipefail

if [[ -e /run/systemd/shutdown/scheduled ]]; then
  exec bunny-cmd-shutdown
fi

if ! pgrep --exact hyprlock >/dev/null; then
  bunny-lock-screen &
  sleep 0.5
fi

systemctl suspend
